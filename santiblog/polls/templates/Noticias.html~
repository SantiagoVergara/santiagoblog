{% extends 'base.html' %}
{% load staticfiles %}
{% block contenido %}

<html>
    <body>

        <br>
        <div class="row">
<a class='dropdown-button btn' href='#' data-activates='dropdown2'>Drop Me!</a>
  <ul id='dropdown2' class='dropdown-content'>
  {% for categoria in categorias %}
    <li><a href="{% url 'mclovin:categoria' categoria.id %}">{{categoria.titulo_txt}}</a></li>
  {% endfor %}
  </ul>

        {% for noticia in noticias %}
                <div class="col s4 card-panel blue-grey lighten-5 grid-example"><span class="flow-text">
                  <div class="card">
                    <div class="card-image waves-effect waves-block waves-light">
                      <img class="activator" src="/static/img/img3.jpg">
                    </div>

                    <div class="card-content">
                      <span class="card-title activator grey-text text-darken-4"><a href="{% url 'mclovin:ver_post' noticia.id %}">{{noticia.titulo_txt}}</a> <i class="mdi-navigation-more-vert right"></i></span>
                      <p><a href="#"></a></p>
                    </div>
                    <div class="card-reveal">
                      <span class="card-title grey-text text-darken-4"><i class="mdi-navigation-close right"></i></span>
                      <p>{{noticia.txt_resumen}}</p>
                    </div>
                  </div>
              </span>
            </div>
        {% endfor %} 
        
        <script src="js/jquery.min.js"></script>
        <script src="js/materialize.min.js"></script>

        <script>
<br>
        $('.dropdown-button').dropdown({
      inDuration: 300,
      outDuration: 225,
      constrain_width: false, // Does not change width of dropdown to that of the activator
      hover: true, // Activate on hover
      gutter: 0, // Spacing from edge
      belowOrigin: false // Displays dropdown below the button
    }
  );</script>

	</body>
</html>
{% endblock %}
